// Clase que representa la estructura de un contenedor de elementos
export class ElemnrtContainer {
    constructor() {
      // 0: horizontal, 1: vertical
      this.direccion = 0; 
      this.color_fondo = "#000";
      this.grap = 1;
    }
    return_menu() {
      // Aquí definirías la estructura del menú
      let estructura_menu = "Estructura de Menú: *falta definir*";
      return estructura_menu;
    }
    return_panel() {
      // Aquí definirías la estructura del panel
      let estructura_panel = "Estructura de Panel: *falta definir*";
      return estructura_panel;
    }
    return_zona_dragable() {
      // Aquí definirías la estructura de la zona dragable
      let estructura_zona = "Estructura de Zona Dragable: *falta definir*";
      return estructura_zona;
    }
  }